apiVersion: v1
kind: Service
metadata:
  labels:
    app: verdaccio
  name: verdaccio-service
  namespace: jaden-ci
spec:
  clusterIP: None
  ports:
  - port: 4873
    protocol: TCP
    targetPort: 4873
    name: verdaccio
  selector:
    app: verdaccio
  sessionAffinity: None
  type: ClusterIP